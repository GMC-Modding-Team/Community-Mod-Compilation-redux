name: TOC Generator

on:
  push:
    branches:
    - Experimental-Mod-Pack
    paths:
    - '**.md'

jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v3
